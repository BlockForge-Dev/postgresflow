# Runtime DB (docker compose uses this)
# DATABASE_URL=postgres://postgres:postgres@db:5432/postgresflow_dev

# Integration tests DB
# TEST_DATABASE_URL=postgres://postgres:postgres@db:5432/postgresflow_test

# Worker config
# PGFLOW_WORKER_ID=worker-1
# (leave empty to use container hostname)
# PGFLOW_QUEUE=default
# PGFLOW_LEASE_SECONDS=10
# PGFLOW_DEQUEUE_BATCH_SIZE=512
# PGFLOW_REAP_INTERVAL_MS=5000
# PGFLOW_VERBOSE_JOB_LOGS=0

# Admin API (set to "off" to disable)
# PGFLOW_ADMIN_ADDR=0.0.0.0:3003
# PGFLOW_API_TOKEN=change-me

# Enqueue guardrails
# PGFLOW_MAX_PAYLOAD_BYTES=262144
# PGFLOW_MAX_ENQUEUE_PER_MINUTE=10000

# Optional: run migrations on startup
# PGFLOW_MIGRATE_ON_STARTUP=1

# DB pool/session tuning
# PGFLOW_DB_MAX_CONNECTIONS=4
# PGFLOW_DB_ACQUIRE_TIMEOUT_SECS=10
# PGFLOW_DISABLE_SYNC_COMMIT=0
# PGFLOW_DISABLE_JIT=1

# Optional Postgres server tuning (unsafe durability settings for local benchmarks only)
# PGFLOW_PG_SYNC_COMMIT=on
# PGFLOW_PG_FSYNC=on
# PGFLOW_PG_FULL_PAGE_WRITES=on
# PGFLOW_PG_WAL_COMPRESSION=on
